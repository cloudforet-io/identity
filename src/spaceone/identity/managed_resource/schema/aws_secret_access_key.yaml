---
schema_id: aws-secret-access-key
name: AWS Access Key
version: '1.0'
schema_type: SECRET
schema:
  order:
  - aws_access_key_id
  - aws_secret_access_key
  properties:
    aws_access_key_id:
      format: password
      minLength: 4.0
      title: AWS Access Key
      type: string
    aws_secret_access_key:
      format: password
      minLength: 4.0
      title: AWS Secret Key
      type: string
  required:
  - aws_access_key_id
  - aws_secret_access_key
  type: object
provider: aws